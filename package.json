{
  "name": "hotel-damage-app",
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix client run build",
    "seed": "node server/database/seed.js",
    "start": "node server/index.js",
    "render-build": "npm --prefix server install && npm --prefix client install && npm --prefix client run build && node server/database/seed.js"
  }
}
